# 项目协作与记忆

- 每次开始处理本项目，以及每次修改代码前，先简要阅读根目录的 `MEMORY.md`，了解当前状态、已有决定和待办事项。
- 同一轮中的连续修改可复用已读内容；切换任务或发现文件有外部更新时重新阅读。
- 修改前检查实际文件和 `git status --short`，必要时查看差异。记忆是交接摘要；如与代码冲突，以实际代码和最新用户要求为准，并修正记忆。
- 每次完成一组代码修改后、结束对话前，更新 `MEMORY.md`：简要记录改了什么、原因、验证结果以及未完成事项。中途交接时也记录进度。
- 区分已验证事实、静态阅读发现和待确认事项；未运行的构建或测试不得记录为通过。
- 保持记忆精简，更新过时状态，近期记录仅保留有助于后续工作的内容。不要保存密钥、凭据或无关个人信息。
- 保留用户已有的未提交修改，不擅自覆盖、回滚或提交。
- 编码遵循用户提供的个人全局代码风格及本项目既有约定；优先可读性、可维护性和正确性。
